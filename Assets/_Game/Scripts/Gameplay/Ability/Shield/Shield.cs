using MEC;
using Obvious.Soap;
using UnityEngine;

public class Shield : MonoBehaviour
{
    // [SerializeField] private FloatReference _speed;
    // [SerializeField] private FloatReference _speedMultiplier;
    //
    // private void Start()
    // {
    //     Timing.RunCoroutine(Utility.EmulateUpdate(MyUpdate, this).CancelWith(gameObject));
    // }
    //
    
    // private void OnTriggerEnter(Collider other)
    // {
    //     var enemy = other.GetComponent<Enemy>();
    //     enemy.TakeDamage(40f);
    // }
    //
    // private void MyUpdate()
    // {
    //     transform.RotateAround(transform.parent.transform.position, Vector3.up,
    //         _speed.Value * _speedMultiplier * Time.deltaTime);
    // }
}
